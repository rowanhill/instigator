{
  "name": "instigator",
  "version": "0.2.0",
  "description": "A minimal memoization / observer pattern library",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "jest": "jest --watch",
    "test": "jest"
  },
  "author": "Rowan Hill",
  "license": "ISC",
  "dependencies": {
    "shallow-equals": "^1.0.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/shallow-equals": "^1.0.0",
    "jest": "^24.7.1",
    "ts-jest": "^24.0.2",
    "typescript": "^3.4.3"
  }
}
